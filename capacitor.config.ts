import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pigmentmixer.app',
  appName: 'vite_react_shadcn_ts',
  webDir: 'dist'
};

export default config;
